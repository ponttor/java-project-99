spring:
  datasource:
    url: jdbc:h2:mem:app_development;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true

jwt:
  secret: ${JWT_SECRET:local-development-secret-key-change-in-production}
